import { Title, TitleType } from "../components";

const App: React.FC = () => {
  return (
    <div>
      <Title content="Simplex-Software.io" type={TitleType.One} />
    </div>
  );
};

export default App;
