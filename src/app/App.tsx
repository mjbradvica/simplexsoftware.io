import { Title, TitleType } from "../components";

const App: React.FC = () => {
  return (
    <div>
      <Title type={TitleType.One} content="Simplex-Software.io" />
    </div>
  );
};

export default App;
